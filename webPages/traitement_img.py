# ======================================================================================================================
# Import
# ======================================================================================================================
from PIL import Image

# ======================================================================================================================
# Récupération de l'image
# ======================================================================================================================
path = 'membres/avatars/1.png'  # Entrer le chemin de l'image utiliser
img = Image.open(path)

# ======================================================================================================================
# Redimention de l'image
# ======================================================================================================================
size = (606, 606)  # Rentrer le format voulu sous la forme d'un tuple


def Redimention(size, img):
    """
    :return im = .png, .jpeg | format(size)
    """

    im = img.resize(size)
    return im


# Execution de la fonction ?
Redimention(size, img)
